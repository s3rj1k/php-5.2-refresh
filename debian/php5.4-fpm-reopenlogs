#!/bin/sh
CONFFILE=/etc/php/5.4/fpm/php.ini
[ -r /etc/default/php5.4-fpm ] && . /etc/default/php5.4-fpm
PIDFILE=/var/run/php5.4-fpm.pid
[ -r "$PIDFILE" ] && kill -USR1 $(cat "$PIDFILE") > /dev/null
exit 0
